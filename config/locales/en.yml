en:
  global: #to use this full notation needs to be used as we don't use existing routes (global.navbar.test)
    navbar:
      test: "testing again"

  pages: #controller (plural)
    home: #view
      test: "Testing" #key used
      #test_html in case your content contains html tags

  #translating models

  activerecord:
    errors:
      models:
        formsubmission:
          attributes:
            email:
              blank: "cannot be empty"
    attributes:
      booking: #always singular
        email: "Email address"
        phone: "Phone number"
        adults: "Number of adults"
        childrens: "Number of children "
        baby_bed: " Do you need a baby bed?"
        street: "Street name and house number"
        city: "City"
        zipcode: "Postal code"
        country: "Country"
        comment: "Insert any additional comments"
        arrival_date: "Arrival date"
        departure_date: "Departure date"

  # <%= f.input :email %>
  # <%= f.input :phone %>
  # <%= f.input :adults, collection: 1..5 %>
  # <%= f.input :childrens, collection: 1..5 %>
  # <%=f.input_field :baby_bed, as: :boolean, boolean_style: :inline, label: t(".baby_bed")  %>
  # <%= f.input :street %>
  # <%= f.input :city %>
  # <%= f.input :zipcode %>
  # <%= f.input :country %>
  # <%= f.input :comment %>
  # <%= f.input :arrival_date, as: :string, input_html: {class: "datepicker"} %>
  # <%= f.input :departure_date, as: :string, input_html: {class: "datepicker"} %>

